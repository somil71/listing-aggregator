# TEMPLATE — do NOT commit real values.
# Create the real secret with:
#
#   kubectl create secret generic property-digest-secrets \
#     --namespace property-digest \
#     --from-literal=CLERK_PUBLISHABLE_KEY=pk_live_... \
#     --from-literal=CLERK_SECRET_KEY=sk_live_... \
#     --from-literal=CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----..." \
#     --from-literal=DATABASE_URL=postgresql://... \
#     --from-literal=UPSTASH_REDIS_REST_URL=https://... \
#     --from-literal=UPSTASH_REDIS_REST_TOKEN=... \
#     --from-literal=GROQ_API_KEY=gsk_...
#
# Or use Sealed Secrets / External Secrets Operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: property-digest-secrets
  namespace: property-digest
type: Opaque
stringData:
  # Clerk auth
  CLERK_PUBLISHABLE_KEY: "REPLACE_ME"
  CLERK_SECRET_KEY:      "REPLACE_ME"
  CLERK_JWT_KEY:         "REPLACE_ME"
  # Postgres (Neon or self-hosted)
  DATABASE_URL:          "REPLACE_ME"
  # Upstash Redis (parse queue)
  UPSTASH_REDIS_REST_URL:   "REPLACE_ME"
  UPSTASH_REDIS_REST_TOKEN: "REPLACE_ME"
  # Groq LLM (free tier)
  GROQ_API_KEY:          "REPLACE_ME"
